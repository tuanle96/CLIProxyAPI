interface:
  display_name: "CLIProxyAPI"
  short_description: "Repo-specific patterns and workflows for CLIProxyAPI"
  default_prompt: "Use the CLIProxyAPI repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true